// import { fonts } from ".";

const theme = {
   fonts: {
      sans: 'var(--font-sans-serif)',
      sansSerif: 'var(--font-sans-serif)',
      monospace: 'var(--font-sans-serif)',
   },
   colors: {
      primary: 'blue',
   },
}

export default theme
